#include <stdio.h>

int main()
{
	int i = 2;
	int project = 1;
	int n = 1;
	int n2 = 0;
	int sum = 0;
	scanf("%d", &n2);
	for (n = 1; n < n2 + 1; n++)
	{
		for (project = 1, i = 2; i < n + 1; i++)
		{
			project = project * i;
		}
		sum = sum + project;
	}
	printf("%d\n",sum);
	return 0;
}
