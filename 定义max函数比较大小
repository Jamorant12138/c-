#include <stdio.h>
//此代码使用定义比较函数max的方法
int max(int x, int y)//定义函数时要用大括号
//全局变量的声明
{
	if (x >= y)
	{
		printf("较大数为%d\n", x);
		return 0;
	}
	else
	{
		printf("较大数为%d\n", y);
		return 0;
	}
}
int main()
{
	int x = 0;
	int y = 0;
	scanf("%d%d", &x, &y);
	max(x, y);
	return 0;
}
